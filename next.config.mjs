/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["res.cloudinary.com", "encrypted-tbn2.gstatic.com"], // Add all your external image domains here
  },
};

export default nextConfig;
